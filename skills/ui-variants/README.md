# ui-variants

Category: **exploration**
Estimated context cost: **medium**

Stages: scope one piece → recon tokens → name divergent directions → build isolated picker → verify and hand off → promote the pick

Curated for AI Agent Stack from Emil Kowalski's `prototype` skill (`emilkowalski/skills`). Adapted from Emil's `prototype` skill (renamed: the stack already ships a `prototype` skill). Builds divergent working variants behind a live picker. Trigger-gated so it only loads when a task names it.
