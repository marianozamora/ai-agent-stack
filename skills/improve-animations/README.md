# improve-animations

Category: **quality**
Estimated context cost: **medium**

Stages: recon → audit eight categories → vet and prioritize → wait for selection → write self-contained plans

Curated for AI Agent Stack from Emil Kowalski's `improve-animations` skill (`emilkowalski/skills`). Audits a codebase's motion and writes self-contained implementation plans for other agents; never edits source. Trigger-gated so it only loads when a task names it.
