# animate-expo

Category: **design**
Estimated context cost: **medium**

Stages: frequency gate → name the purpose → cheapest tool → properties → timing or spring → stay on the ui thread → press and haptics → reduced motion

Curated for AI Agent Stack from Emil Kowalski's `animate-expo` skill (`emilkowalski/skills`). The React Native/Expo counterpart to `motion-design`: Reanimated, Gesture Handler and haptics, judged on a release build. Trigger-gated so it only loads when a task names it.
