# minimalist-ui

Category: **design**
Estimated context cost: **low**

Stages: macro-whitespace first → editorial type hierarchy → warm monochrome palette → flat bordered components → quiet motion

Curated for AI Agent Stack from Leonxlnx's `minimalist-skill` skill (`taste-skill`). Warm, editorial, document-style interfaces: monochrome palette, serif headings, flat bordered bento grids, muted pastel accents. Trigger-gated so it only loads when a task names it.
