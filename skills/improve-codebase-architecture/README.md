# improve-codebase-architecture

Category: **quality**
Estimated context cost: **medium**

Stages: scope from hot spots → apply deletion test → render before/after report → grill the chosen candidate → sync domain model and ADRs

Curated for AI Agent Stack from Matt Pocock's `improve-codebase-architecture` skill. A periodic, whole-codebase audit for deepening opportunities, using the same vocabulary `codebase-design` uses inline while implementing (seam, depth, the deletion test) — that skill applies the vocabulary as you go; this one goes looking for where it's missing.
