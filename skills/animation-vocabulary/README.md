# animation-vocabulary

Category: **design**
Estimated context cost: **tiny**

Stages: read for sensation → name the term → disambiguate alternates

Curated for AI Agent Stack from Emil Kowalski's `animation-vocabulary` skill (`emilkowalski/skills`). A reverse-lookup glossary that names a loosely described motion effect so the user knows what to ask for. Trigger-gated so it only loads when a task names it.
