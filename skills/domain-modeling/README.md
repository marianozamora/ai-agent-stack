# domain-modeling

Category: **elicitation**
Estimated context cost: **low**

Stages: challenge glossary → sharpen fuzzy terms → test with scenarios → cross-reference code → update CONTEXT.md inline → offer ADR sparingly

Curated for AI Agent Stack from Matt Pocock's `domain-modeling` skill. Pairs with `grilling`: grilling drives the question round, this skill is what keeps the vocabulary it produces (`CONTEXT.md`, ADRs) precise and current as the conversation goes.
