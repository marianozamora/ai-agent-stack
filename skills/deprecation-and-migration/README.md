# deprecation-and-migration

Category: **migration**
Estimated context cost: **medium**

Stages: inventory contracts → choose compatibility policy → migrate incrementally → verify rollback → remove when proven

Curated for AI Agent Stack from Addy Osmani's `deprecation-and-migration` workflow. It complements `wayfinder` by governing execution after the migration direction is chosen.
