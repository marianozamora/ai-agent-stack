# sonner

Category: **tooling**
Estimated context cost: **low**

Stages: one toaster → pick the call → styling ladder → troubleshoot by symptom

Curated for AI Agent Stack from Emil Kowalski's `ask-sonner` skill (`emilkowalski/skills`). Adapted from `ask-sonner`: setup, API choice, styling and troubleshooting for the Sonner toast library. Trigger-gated so it only loads when a task names it.
