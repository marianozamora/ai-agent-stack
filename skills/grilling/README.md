# grilling

Category: **elicitation**
Estimated context cost: **medium**

Stages: map design tree → ask frontier round → wait for answers → recompute frontier → confirm shared understanding

Curated for AI Agent Stack from Matt Pocock's `grilling` skill (upstream also ships a `grill-me` alias that just calls into `grilling`; here that's folded into this skill's own trigger list instead of a separate skill file). Activates on explicit stress-test requests, never on ordinary planning.
