# high-end-visual-design

Category: **design**
Estimated context cost: **medium**

Stages: choose vibe and layout archetype → scaffold whitespace and type → double-bezel architecture → choreograph motion → performance guardrails → pre-output checklist

Curated for AI Agent Stack from Leonxlnx's `soft-skill` skill (`taste-skill`). Adapted from `high-end-visual-design`: agency-level sites built from a chosen vibe and layout archetype with nested "double-bezel" components. Trigger-gated so it only loads when a task names it.
