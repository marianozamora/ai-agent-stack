# anti-slop-frontend-v1

Category: **design**
Estimated context cost: **medium**

Stages: fixed dials → verify dependencies → bias-corrected typography and color → full interaction states → performance guardrails → pre-flight check

Curated for AI Agent Stack from Leonxlnx's `taste-skill-v1` skill (`taste-skill`). The original v1 taste skill, kept for projects that depend on its exact rules; `anti-slop-frontend` is the current version. Trigger-gated so it only loads when a task names it.
