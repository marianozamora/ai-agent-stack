Before reaching for a tool named in the Context order below, confirm it is listed there -- anything not listed is not installed here and must not be assumed. Reuse a tool's own cached output (a graph already built, an index already synced) instead of re-querying facts already fetched this task. Escalate to a broader or slower tool only after a narrower one fails to answer the specific question.
