# brutalist-ui

Category: **design**
Estimated context cost: **low**

Stages: pick one archetype → typographic structure → single hazard accent → rigid compartmentalized grid → analog texture

Curated for AI Agent Stack from Leonxlnx's `brutalist-skill` skill (`taste-skill`). Adapted from `industrial-brutalist-ui`: Swiss industrial print or tactical terminal interfaces with rigid grids and extreme type contrast. Trigger-gated so it only loads when a task names it.
