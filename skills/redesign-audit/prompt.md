Upgrade an existing UI without rewriting it. **Scan**: identify the framework, the styling system (check Tailwind v3 vs v4) and current patterns. **Diagnose**, citing `file:line`, the generic AI fingerprints: browser-default or Inter-everywhere type, headlines without weight contrast, body text wider than ~65ch; pure `#000`, oversaturated or multiple accents, purple-blue gradients, untinted shadows; everything centered, three equal feature cards, `100vh` instead of `100dvh`; missing hover/active/focus, loading, empty and error states, dead `#` links; placeholder content (Lorem ipsum, John Doe, 99.99%, "Elevate"/"Seamless" copy), div soup, missing alt text.

**Fix** lowest-risk first: font, palette, interaction states, layout and spacing, generic components, missing states, type scale. Stay on the existing stack, check the dependency file before importing anything, keep each change reviewable, and re-run the app or tests after each step. Close with what changed per category and what was deliberately left alone.
