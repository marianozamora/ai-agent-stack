# redesign-audit

Category: **design**
Estimated context cost: **medium**

Stages: scan stack and styling → diagnose generic patterns → fix lowest-risk first → verify after each step → report per category

Curated for AI Agent Stack from Leonxlnx's `redesign-existing-projects` skill (`taste-skill`). An audit-then-fix pass that removes generic "AI-made" UI patterns from an existing project in a fixed, low-risk order, without migrating its stack. Trigger-gated so ordinary feature work never pulls in an aesthetic audit.
