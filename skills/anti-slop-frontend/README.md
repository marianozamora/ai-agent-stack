# anti-slop-frontend

Category: **design**
Estimated context cost: **medium**

Stages: design read → set the three dials → choose a design system → detect redesign mode → sweep ai tells → pre-flight check

Curated for AI Agent Stack from Leonxlnx's `taste-skill` skill (`taste-skill`). The current (v2) taste skill: brief inference, three dials and a strict pre-flight for landing pages, portfolios and marketing redesigns. Trigger-gated so it only loads when a task names it.
