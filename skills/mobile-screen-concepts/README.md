# mobile-screen-concepts

Category: **design**
Estimated context cost: **high**

Stages: platform mode → infer screen count → lock a design bible → order a believable flow → generate every screen → quality check

Curated for AI Agent Stack from Leonxlnx's `imagegen-frontend-mobile` skill (`taste-skill`). Adapted from `imagegen-frontend-mobile`: premium, app-native mobile screen and flow images (images only, no code). Trigger-gated so it only loads when a task names it.
