# find-animation-opportunities

Category: **design**
Estimated context cost: **low**

Stages: recon → sweep seams → four-question gate → report opportunities → report rejections → verdict

Curated for AI Agent Stack from Emil Kowalski's `find-animation-opportunities` skill (`emilkowalski/skills`). Read-only search for places that should animate, filtered hard by frequency, purpose, speed and function. Trigger-gated so it only loads when a task names it.
