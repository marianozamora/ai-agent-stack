# review-animations

Category: **quality**
Estimated context cost: **low**

Stages: measure against ten standards → escalate on sight → rank remedies → before/after table → block or approve

Curated for AI Agent Stack from Emil Kowalski's `review-animations` skill. A motion-only review with an explicit Block/Approve verdict. Upstream it only runs when invoked by name; here it is trigger-gated the same way. Distinct from the `design` reviewer gate `ai pipeline` runs for Figma work.
