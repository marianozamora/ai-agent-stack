# stitch-design-md

Category: **design**
Estimated context cost: **low**

Stages: define atmosphere → map palette with roles → typography rules → component stylings → layout and responsive rules → motion → anti-patterns

Curated for AI Agent Stack from Leonxlnx's `stitch-skill` skill (`taste-skill`). Writes a `DESIGN.md` in the semantic style Google Stitch (and other screen-generating agents) can follow. Trigger-gated so it only loads when a task names it.
