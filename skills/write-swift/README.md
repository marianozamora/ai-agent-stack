# write-swift

Category: **implementation**
Estimated context cost: **medium**

Stages: value types first → visible failure paths → main actor until profiled → generics before existentials → measure then optimize → migrate per target

Curated for AI Agent Stack from Emil Kowalski's `write-swift` skill (`emilkowalski/skills`). Modern Swift (6.x) guidance: value types, approachable concurrency, API design, performance and Swift Testing. Trigger-gated so it only loads when a task names it.
