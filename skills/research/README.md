# research

Category: **evidence**
Estimated context cost: **low**

Stages: scope question → dispatch background agent → verify primary sources → write findings → cite sources

Curated for AI Agent Stack from Matt Pocock's `research` skill. Complements `wayfinder` and `to-tickets` by delegating primary-source reading to a background agent instead of consuming the main conversation's context.
