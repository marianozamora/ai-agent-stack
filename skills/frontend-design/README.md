# frontend-design

Category: **design**
Estimated context cost: **medium**

Stages: name the surface mode → read product and design context → honor the brief → preserve or replace deliberately → cover real states → bounded verification pass

Curated for AI Agent Stack from Paul Bakaus's `impeccable` skill (Apache-2.0). Keeps its surface modes, brief-first and refine-vs-redesign rules and bounded verification; drops the launcher binary, the sub-command catalog and the reference playbooks. Primary for `design` tasks (a Figma link), and reachable from feature tasks through its triggers.
