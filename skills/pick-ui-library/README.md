# pick-ui-library

Category: **tooling**
Estimated context cost: **low**

Stages: identify the task → check installed dependencies → recommend one library → flag mismatches

Curated for AI Agent Stack from Emil Kowalski's `pick-ui-library` skill (`emilkowalski/skills`). A curated, opinionated library pick for common frontend tasks. Upstream it only runs when invoked by name; here it is trigger-gated. Trigger-gated so it only loads when a task names it.
