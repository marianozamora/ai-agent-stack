Build a motion-rich marketing page with GSAP (`@gsap/react`, ScrollTrigger). Before code, output a `<design_plan>`: a deterministic pick, seeded from the prompt length, of one hero layout, one font (Satoshi, Cabinet Grotesk, Outfit or Geist, never Inter), three component patterns and two scroll paradigms; an AIDA check (nav, hero, interest/bento, desire/scroll, action/footer); the H1 `max-w` that keeps it to 2-3 lines; bento math proving no empty cells with `grid-flow-dense`; a sweep for meta-labels ("SECTION 01") and button contrast.

Sections get cinematic spacing (`py-32 md:py-48`). Hero: wide container, two legible CTAs, no badges, pills or stats. Use 3-5 intentional bento cards. Motion: a pinned title beside a scrolling gallery, scale-in images, scrubbed text reveals, stacking cards, hover scale inside `overflow-hidden`, all behind `prefers-reduced-motion`. Wrap the page in `overflow-x-hidden`. No emojis; seeded picsum imagery with CSS filters.
