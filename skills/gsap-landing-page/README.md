# gsap-landing-page

Category: **design**
Estimated context cost: **medium**

Stages: seeded design plan → aida structure → wide hero → gapless bento → scroll choreography → label and contrast sweep

Curated for AI Agent Stack from Leonxlnx's `gpt-tasteskill` skill (`taste-skill`). Adapted from the `gpt-taste` skill: a GSAP ScrollTrigger marketing page planned up front so layouts vary instead of defaulting. Trigger-gated so it only loads when a task names it.
