# web-section-concepts

Category: **design**
Estimated context cost: **high**

Stages: infer section count → commit to one image per section → lock one brand world → vary composition per section → clarity check

Curated for AI Agent Stack from Leonxlnx's `imagegen-frontend-web` skill (`taste-skill`). Adapted from `imagegen-frontend-web`: website design reference images, one horizontal image per section (images only, no code). Trigger-gated so it only loads when a task names it.
