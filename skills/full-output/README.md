# full-output

Category: **efficiency**
Estimated context cost: **low**

Stages: lock deliverable count → build every deliverable → cross-check against the request → pause at a clean boundary

Curated for AI Agent Stack from Leonxlnx's `output-skill` skill (`taste-skill`). Adapted from `full-output-enforcement`: complete, placeholder-free output with a clean pause protocol when it cannot fit. Trigger-gated so it only loads when a task names it.
