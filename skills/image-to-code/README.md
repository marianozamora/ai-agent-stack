# image-to-code

Category: **design**
Estimated context cost: **high**

Stages: infer sections → generate one image per section → deep image analysis → extract the design spec → implement without drift

Curated for AI Agent Stack from Leonxlnx's `image-to-code-skill` skill (`taste-skill`). Image-first web design: generate section references, analyze them into a spec, then implement to match. Trigger-gated so it only loads when a task names it.
