# code-review

Category: **quality**
Estimated context cost: **medium**

Stages: pin fixed point → identify spec and standards sources → apply smell baseline → parallel standards + spec subagents → aggregate without reranking

Curated for AI Agent Stack from Matt Pocock's `code-review` skill. An ad-hoc, two-axis review (repo standards vs. the originating spec) for a branch or diff the user names directly — distinct from the `review`/`security`/`design` reviewer gates `ai pipeline` runs, which judge against the PR contract instead.
