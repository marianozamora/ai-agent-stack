# resolving-merge-conflicts

Category: **version-control**
Estimated context cost: **medium**

Stages: survey state → find primary sources → resolve hunks → run checks → finish merge

Curated for AI Agent Stack from Matt Pocock's `resolving-merge-conflicts` skill. It activates only when the task explicitly involves an in-progress git merge or rebase conflict, and never bails out with `--abort`.
