"""AI Agent Stack.

This package is importable (for the ``ai`` console entry point and packaging),
but its modules use flat, same-directory imports and are also run as plain
scripts. cli.py prepends this directory to sys.path so both styles resolve.
"""
