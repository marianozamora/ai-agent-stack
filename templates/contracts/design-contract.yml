version: 1
enabled: true
source: figma
source_url: ""
file_key: ""
node_id: ""
frame_name: ""

# Filled from Figma MCP. Keep this compact: it is a contract, not raw MCP output.
components:
  required_reuse: []
  unmapped: []

tokens:
  required: []
  hardcoded_values_forbidden: true

states:
  required: []

responsive:
  required: []

interactions:
  required: []

assets:
  required: []
  prefer_original_assets: true
  never_recreate_existing_assets: true

code_connect:
  preferred: true
  mappings_checked: false

fidelity:
  layout: required
  typography: required
  spacing: required
  component_variants: required
  responsive_behavior: required
  interaction_states: required
  pixel_nitpicks_blocking: false

missing_design_information: []
