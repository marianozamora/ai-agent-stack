{
  "version": 1,
  "id": "budget-exhaustion",
  "task": "Small change to trigger a scripted usage overrun",
  "profiles": ["fast"],
  "base_files": {"README.md": "# Demo app\n"},
  "change_files": {"src/util/format.py": "def fmt(x):\n    return str(x)\n"},
  "acceptance": ["fmt() returns a string"],
  "synthetic": {
    "cleanup": {"status": "PASS", "evidence": ["No debug residue"],
                "usage": {"input_tokens": 30000, "output_tokens": 20000}}
  },
  "expect": {"risk": "LOW", "readiness": "NEEDS_HUMAN",
             "pipeline_message_contains": "usage budget exceeded"}
}
